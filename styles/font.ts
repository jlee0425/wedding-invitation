import { East_Sea_Dokdo } from "@next/font/google";

const eastSeaDokdo = East_Sea_Dokdo({
  weight: "400",
  subsets: ["korean", "latin"],
  variable: "--font-east-sea-dokdo",
  preload: true,
  display: "block",
});

export default eastSeaDokdo;
